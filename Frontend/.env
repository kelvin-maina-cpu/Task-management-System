# API Configuration - Development
# The Vite proxy handles routing /api requests to the backend
# Leave empty to use proxy (vite.config.ts handles /api routing)
VITE_API_URL=/api
VITE_BACKEND_URL=http://localhost:5000

